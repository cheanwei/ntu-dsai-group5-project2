# Seeds

No dbt seeds are currently used. The product-category translation file is one
of the nine Kaggle source files and is loaded into `olist_raw` by dlt.

If a small, repository-owned lookup table is added later, place it here and
reference it with dbt's `ref()`.
