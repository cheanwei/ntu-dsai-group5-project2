# Dashboard-only dependencies for the Cloud Run image (see README.md).
# The repo-root uv.lock also carries dagster, dbt and jupyter (~2 GB), none of
# which the dashboard imports. Keep these bounds in step with pyproject.toml.
streamlit>=1.62
pandas>=2.2,<3
plotly>=7.1
folium>=0.20
streamlit-folium>=0.27.4
requests>=2.32
flask>=3.1.3
sqlalchemy>=2.0
sqlalchemy-bigquery>=1.17
google-cloud-bigquery-storage>=2.27
python-dotenv>=1.0
