
version: 2

models:
  - name: dim_product
    description: "Dimensional table for the products"
    columns:
      - name: product_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

